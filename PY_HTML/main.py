import eel
import pygame

#pygame.init()
#screen = pygame.display.set_mode((0, 0), pygame.FULLSCREEN)
#window_width, window_height = screen.get_size()
eel.init("web")
eel.start("index.html",size=(1920 ,1080))
# how take the size of the window
